import { AI_NAME, OWNER_NAME } from "@/configuration/identity";

export const CHAT_HEADER: string = `${OWNER_NAME}'s AI Assistant Jarvis`;
export const MESSAGE_PLACEHOLDER: string = `What would you like to ask about Jonathan??`;
export const FOOTER_MESSAGE: string = `${OWNER_NAME} 2025`;
export const CLEAR_BUTTON_TEXT: string = `Clear Text`;
export const PAGE_TITLE: string = `${OWNER_NAME}'s AI`;
export const PAGE_DESCRIPTION: string = `Chat with ${AI_NAME}, ${OWNER_NAME}'s AI assistant.`;

export const EMPTY_CITATION_MESSAGE: string = "Unspecified source";
